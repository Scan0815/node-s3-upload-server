{
  "name": "node-s3-api-server",
  "version": "1.0.0",
  "description": "4based",
  "main": "s3-api-server",
  "scripts": {
    "start": "npm run build && npm run server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc -p tsconfig.json",
    "server": "node build/server.js",
    "clean": "rimraf ./build",
    "start-continuous": "supervisor npm run server"
  },
  "dependencies": {
    "aws-sdk": "^2.1100.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "lodash": "^4.17.21",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.1"
  },
  "keywords": [],
  "author": "4based",
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "@types/lodash": "^4.14.186",
    "rimraf": "^3.0.2"
  }
}
